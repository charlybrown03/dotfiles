#!/bin/sh

# Debian distrubutions
# sudo apt install autojump git htop node wget atom docker vagrant virtualbox xsel

# Arch distrubutions
sudo pacman -Sy autojump git htop node wget atom docker vagrant virtualbox xsel
